# ChatUI 모듈 PRD (Product Requirements Document)

## 문서 정보
- **문서명**: ChatUI 제품 요구사항 명세서
- **버전**: 1.0
- **작성일**: 2025-01-31

---

## 1. 개요

### 1.1 제품 개요
**ChatUI**는 프레임워크에 의존하지 않는 재사용 가능한 채팅 UI 모듈이다. React, Vue 등 외부 라이브러리 없이 **바닐라 JavaScript** 한 파일(`chat-ui.js`)로 동작하며, 다양한 웹 프로젝트(챗봇, AI 어시스턴트, 고객 지원 등)에 쉽게 임베딩할 수 있다.

### 1.2 목표
- **재사용성**: 한 파일로 복사하여 즉시 사용 가능
- **경량성**: 별도 번들러/빌드 없이 `<script>` 태그로 로드
- **확장성**: 이벤트 기반 API로 백엔드/API 연동 용이
- **커스터마이징**: 테마, 배경, 프로필 등 시각적 설정 지원

### 1.3 타겟 사용자
- 챗봇/AI 어시스턴트 웹 개발자
- 고객 지원 채팅 UI를 빠르게 구축하려는 팀
- 기존 프로젝트에 채팅 UI를 추가하려는 개발자

---

## 2. 기술 요구사항

### 2.1 구현 형식

| ID | 요구사항 | 설명 |
|----|----------|------|
| TR-001 | **JavaScript Class** | ChatUI는 반드시 JavaScript `class` 문법으로 구현해야 한다. |
| TR-002 | **클래스 기반 모듈화** | 모든 기능(메시지 표시, 입력 처리, 이벤트, 스타일 주입 등)은 ChatUI 클래스 내부에 메서드·속성으로 모듈화하여 개발해야 한다. |
| TR-003 | **단일 JS 파일** | 구현물은 **chat-ui.js** 파일 1개로 완성되어야 한다. 별도 JS 파일 분리 금지. |
| TR-004 | **단일 HTML 파일** | 테스트 및 데모는 **index.html** 파일 1개로 제공해야 한다. |
| TR-005 | **파일 구성** | ChatUI 모듈의 최소 구현은 `chat-ui.js` + `index.html` 두 파일만으로 동작해야 한다. |

### 2.2 공통 모듈 설계

| ID | 요구사항 | 설명 |
|----|----------|------|
| TR-006 | **재사용성** | 다른 프로젝트에서 `<script src="chat-ui.js">` 또는 모듈 import로 바로 사용 가능한 **공통 모듈**로 설계·개발해야 한다. |
| TR-007 | **독립성** | 외부 라이브러리, 프레임워크, 빌드 도구 없이 동작해야 한다. |
| TR-008 | **캡슐화** | ChatUI 클래스가 UI 렌더링, 스타일, 이벤트를 모두 내부에서 관리한다. |

### 2.3 테스트 형식

| ID | 요구사항 | 설명 |
|----|----------|------|
| TR-009 | **HTML 기반 테스트** | 테스트는 HTML(`index.html`)에서 버튼·스크립트로 각 기능을 실행·검증하는 방식으로 수행한다. |
| TR-010 | **테스트 실행** | `index.html`을 브라우저에서 열어 수동/반자동으로 테스트 체크리스트를 실행할 수 있어야 한다. |

---

## 3. 기능 요구사항

### 3.1 핵심 기능

#### 3.1.1 메시지 표시
| 요구사항 ID | 설명 | 우선순위 |
|------------|------|---------|
| F-001 | 사용자(user) / 봇(bot) 역할별 메시지 표시 | P0 |
| F-002 | 텍스트 메시지 표시 | P0 |
| F-003 | 이미지 메시지 표시 (단일/다중) | P0 |
| F-004 | 텍스트 + 이미지 조합 메시지 표시 | P0 |
| F-005 | 메시지별 타임스탬프 저장 및 스크롤 시 툴팁 표시 | P1 |

#### 3.1.2 입력 및 상호작용
| 요구사항 ID | 설명 | 우선순위 |
|------------|------|---------|
| F-006 | 텍스트 입력 필드 및 전송 버튼 | P0 |
| F-007 | Enter 키로 메시지 전송 | P0 |
| F-008 | 선택 버튼(퀵 리플라이) 표시 | P0 |
| F-009 | 선택 버튼 클릭 시 해당 라벨을 사용자 메시지로 자동 추가 (옵션) | P1 |

#### 3.1.3 커스터마이징
| 요구사항 ID | 설명 | 우선순위 |
|------------|------|---------|
| F-010 | 단색 배경 설정 | P1 |
| F-011 | 그라데이션 배경 설정 | P1 |
| F-012 | 이미지 배경 설정 | P1 |
| F-013 | 사용자/봇 프로필(아바타) 이미지 설정 | P1 |
| F-014 | placeholder 텍스트 설정 | P2 |
| F-015 | 테마(theme) 옵션 (현재 pastel) | P1 |

#### 3.1.4 데이터 관리
| 요구사항 ID | 설명 | 우선순위 |
|------------|------|---------|
| F-016 | 메시지 히스토리 로드 (loadHistory) | P0 |
| F-017 | 메시지 히스토리 조회 (getMessages) | P0 |
| F-018 | 메시지 전체 삭제 (clearMessages) | P0 |
| F-019 | 세션 저장 후 복원 지원 | P1 |

### 3.2 이벤트 API

| 이벤트명 | 설명 | payload |
|----------|------|---------|
| `message:send` | 사용자가 메시지 전송 시 | `{ text, role: 'user' }` |
| `choice:selected` | 선택 버튼 클릭 시 | `choice` (id, label, value) |
| `image:click` | 메시지 내 이미지 클릭 시 | `{ src }` |

### 3.3 비기능 요구사항

| ID | 요구사항 |
|----|----------|
| NF-001 | 프레임워크 독립 (Vanilla JS) |
| NF-002 | 별도 CSS 파일 없이 스타일 자동 주입 |
| NF-003 | 모바일 반응형 레이아웃 (768px 미만) |
| NF-004 | ES5+ 호환, Node 모듈 및 전역 스크립트 양쪽 지원 |
| NF-005 | 컨테이너에 CSS 선택자 또는 DOM 요소 전달 가능 |

---

## 4. 기술 스택 및 아키텍처

### 4.1 구성
- **chat-ui.js**: 단일 JS 파일. ChatUI `class` 정의 및 모든 기능 모듈화. (TR-001~TR-005)
- **index.html**: 데모 및 HTML 기반 테스트용 페이지. (TR-009, TR-010)
- **Chat-UI-USERS-GUIDE.md**: 다른 프로젝트 연동용 사용자/개발자 가이드 (선택)

### 4.2 의존성
- 없음 (바닐라 JS, 순수 DOM API)

### 4.3 공개 API (메서드)
- `addMessage(msg, options?)`
- `loadHistory(history)`
- `clearMessages()`
- `getMessages()`
- `setProfile(profiles)`
- `setBackground(config)`
- `resetBackground()`
- `showChoices(data)`
- `hideChoices()`
- `on(event, handler)` / `off(event, handler)`
- `destroy()`

---

## 5. 메시지 객체 스키마

```javascript
{
  role: 'user' | 'bot',       // 기본값: 'bot'
  type: 'text' | 'image',     // 기본값: 'text'
  text?: string,              // 말풍선 텍스트
  images?: Array<{ src: string, alt?: string }>,  // type: 'image' 일 때
  timestamp?: string          // 'YYYY-MM-DD HH:MM:SS' (자동 부여 가능)
}
```

---

## 6. ChatUI 간단 테스트 요구사항

### 6.1 테스트 목적
- 구현된 ChatUI가 요구사항대로 동작하는지 **수동/반자동**으로 검증
- 회귀 테스트 시 빠르게 수행 가능한 체크리스트 제공
- CI/CD 또는 수동 QA 시 참고용

### 6.2 테스트 환경
- **실행 방법**: `index.html`을 로컬 HTTP 서버 또는 `file://`로 열기
- **권장 브라우저**: Chrome, Firefox, Edge, Safari (최신 2개 버전)
- **기기**: 데스크톱 1920px, 태블릿 768px, 모바일 375px

### 6.3 수동 테스트 체크리스트 (HTML 기반)

index.html의 버튼·스크립트를 통해 각 시나리오를 실행하고 결과를 확인한다.

#### 6.3.1 기본 메시지
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-001 | "Bot: Text Message" 클릭 | 봇 텍스트 메시지가 왼쪽에 표시됨 | |
| T-002 | "User: Text Message" 클릭 | 사용자 텍스트 메시지가 오른쪽에 표시됨 | |
| T-003 | 입력 필드에 "hello" 입력 후 전송 버튼 클릭 | 사용자 메시지 "hello" 추가 + Echo 봇 응답 | |
| T-004 | 입력 필드에 "test" 입력 후 Enter | 사용자 메시지 "test" 추가 + Echo 봇 응답 | |
| T-005 | 빈 메시지 전송 시도 | 아무 변화 없음 (전송되지 않음) | |

#### 6.3.2 리치 미디어
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-006 | "Bot: Image Only" 클릭 | 이미지만 포함된 봇 메시지 표시 | |
| T-007 | "Bot: Text + Image" 클릭 | 텍스트 + 이미지 2개가 함께 표시됨 | |
| T-008 | 메시지 내 이미지 클릭 | alert로 "Image clicked: [url]" 표시 | |

#### 6.3.3 선택 버튼
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-009 | "Show Choice Buttons" 클릭 | Yes/No/Maybe 버튼이 입력 영역 위에 표시됨 | |
| T-010 | "Yes, please" 버튼 클릭 | 사용자 메시지로 "Yes, please" 추가 + 봇 응답 + 버튼 숨김 | |
| T-011 | "No, thanks" 버튼 클릭 | 사용자 메시지로 "No, thanks" 추가 + 봇 응답 + 버튼 숨김 | |

#### 6.3.4 커스터마이징
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-012 | "Set Profiles (Avatars)" 클릭 | 사용자/봇 아바타가 외부 이미지로 변경됨 | |
| T-013 | "BG: Gradient" 클릭 | 배경이 그라데이션으로 변경됨 | |
| T-014 | "BG: Image" 클릭 | 배경이 이미지로 변경됨 | |
| T-015 | "Reset BG" 클릭 | 배경이 기본 pastel 테마로 복원됨 | |

#### 6.3.5 시스템/히스토리
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-016 | "Clear Messages" 클릭 | 모든 메시지가 삭제됨 | |
| T-017 | "Show History (Console)" 클릭 | 콘솔에 메시지 배열 출력, alert로 개수 표시 | |
| T-018 | 몇 개 메시지 추가 후 "Save Session" → "Re-create & Load" | ChatUI 재초기화 후 동일한 메시지들이 복원됨 | |

#### 6.3.6 스크롤 및 반응형
| 테스트 ID | 시나리오 | 예상 결과 | 상태 |
|----------|----------|-----------|------|
| T-019 | 메시지 10개 이상 추가 후 스크롤 | 스크롤 시 상단에 가장 가까운 메시지의 시간 툴팁 표시 | |
| T-020 | 새 메시지 추가 시 | 자동으로 맨 아래로 스크롤됨 | |
| T-021 | 뷰포트를 768px 이하로 축소 | 데모 컨트롤과 채팅 영역이 세로 배치, 채팅 영역 full width | |

### 6.4 간단 자동화 테스트 (선택 사항)

프로젝트에 **간단한 자동화 테스트**를 추가할 경우 아래 요구사항을 충족한다.

#### 6.4.1 테스트 프레임워크
- **옵션 A**: Playwright 또는 Puppeteer로 E2E
- **옵션 B**: Jest + jsdom으로 단위 테스트 (DOM 모킹)
- **옵션 C**: 수동 체크리스트만 유지

#### 6.4.2 필수 자동화 시나리오 (옵션 A/B 선택 시)
1. **초기화**: `new ChatUI('#container')` 후 DOM 구조 존재 여부 확인
2. **addMessage**: 텍스트 메시지 추가 후 `.chat-message`, `.chat-bubble` 요소 확인
3. **loadHistory**: `loadHistory([...])` 후 메시지 개수 일치 확인
4. **clearMessages**: `clearMessages()` 후 `.chat-message` 요소 0개 확인
5. **getMessages**: `addMessage` 후 `getMessages()` 반환 배열에 해당 메시지 포함 확인
6. **이벤트**: `message:send` 이벤트 구독 후 전송 시 핸들러 호출 확인

### 6.5 테스트 결과 기록
- 테스트 실행일, 실행자, 브라우저/OS, 결과(Pass/Fail)를 별도 시트 또는 `TEST-RESULTS.md`에 기록
- 회귀 테스트 시 기존 체크리스트 재사용

---

## 7. 성공 지표

| 지표 | 목표 |
|------|------|
| 로드 시간 | `<script>` 로드 후 100ms 이내 초기 렌더링 |
| 번들 크기 | chat-ui.js 50KB 이하 (minify 전) |
| 브라우저 지원 | Chrome, Firefox, Edge, Safari 최신 2버전 |
| 사용성 | Chat-UI-USERS-GUIDE.md만으로 10분 내 통합 가능 |

---

## 8. 향후 확장 (Out of Scope - v1)

- 다크 모드/라이트 모드 토글
- 마크다운 렌더링
- 타이핑 인디케이터
- 파일 첨부
- 음성 메시지
- 읽음 표시(읽음/안 읽음)

---

## 9. 참고 문서

- [Chat-UI-USERS-GUIDE.md](./Chat-UI-USERS-GUIDE.md) - 사용자 및 개발자 연동 가이드
- [index.html](./index.html) - 데모 및 수동 테스트용 페이지
